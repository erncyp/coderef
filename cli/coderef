#!/usr/bin/env python3
# Thin wrapper — all logic lives in coderef_core.py for pip-installability.
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from coderef_core import main  # noqa: E402

if __name__ == "__main__":
    main()
